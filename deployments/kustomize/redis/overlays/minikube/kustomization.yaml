apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: web3

resources:
  - ../../base

patches:
  - path: patch-deployment.yaml
    target:
      kind: StatefulSet
      name: redis
