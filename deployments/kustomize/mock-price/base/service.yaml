apiVersion: v1
kind: Service
metadata:
  name: mock-price-api
  labels:
    app: mock-price-api
spec:
  type: ClusterIP
  ports:
    - port: 4050
      targetPort: 4050
      name: http
  selector:
    app: mock-price-api
