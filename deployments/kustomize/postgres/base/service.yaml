apiVersion: v1
kind: Service
metadata:
  name: auth-postgres
  labels:
    app: auth-postgres
    component: database
spec:
  type: ClusterIP
  selector:
    app: auth-postgres
    component: database
  ports:
    - name: postgres
      protocol: TCP
      port: 5432
      targetPort: postgres
